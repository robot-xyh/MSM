# 数据保留与归档登记

登记日期：2026年9月25日。仓库：`/home/linux/Documents/MSM`。

## 一、本次工作

本次完成实验输出、六个同级目录及保留的未跟踪资料的文件盘点，建立文件校验清单和报告版本登记。没有移动、删除或合并原文件，没有运行仿真、训练模型或重新计算实验结果，也没有提交和推送本轮新增材料。

登记基于分支`exp/d5-ideal-20target-registration`，仓库提交为`c10fb8bf827e2752cf72b03a21a46176598d1d08`。此前的分组提交已经完成，本次保留[提交前的审计记录](../20260925/当前工作进展与证据审计_CN.md)，没有用新状态改写历史。

独立备份尚未建立。已检查的目录都在系统盘上，当前没有确认可用的外置盘或已挂载备份目录。

## 二、盘点结果

| 范围 | 普通文件数 | 逻辑大小 | 已计算SHA-256的文件数 |
| --- | ---: | ---: | ---: |
| 18个实验输出根目录 | 201,746 | 61.40 GiB | 2,347 |
| 六个同级目录 | 35,845 | 2.22 GiB | 35,845 |
| 保留的未跟踪资料 | 203 | 1.27 GiB | 0 |
| 前述范围以外的清单依赖文件 | 5 | 256,972字节 | 5 |
| 合计 | 237,799 | 64.89 GiB | 38,197 |

另登记1个符号链接，只记录其指向，不读取链接目标。按目录形成1,132组记录；分组按目录边界划分，不能当作实验次数。

38,197个文件共读取约2.63 GiB用于计算校验值，未发现读取错误或读取期间内容变化。其余199,602个普通文件仅登记路径、大小和修改时间，没有计算内容校验值。

此前审计中的196条显式来源校验记录，本次按路径去重后仍为196个文件，全部与原记录一致。所扫描范围内有49个模型格式文件，均已计算校验值；该数量不代表49种模型，也不表示文件已加载验证。

上述大小为文件逻辑大小，未按内容或硬链接去重，不等于实际磁盘占用。范围不含主仓库全部源码、所有交付文件及四个外部仓库内部文件。原审计中`research_modules`约63 GiB为整个目录的磁盘占用，统计范围与本表不同。

### 清单核验范围

重点计算校验值的范围包括四个主要结果目录、六个同级目录、模型文件及原审计清单明确引用的输入。完整路径见[逐文件清单](FILES.jsonl.gz)，汇总见[文件盘点摘要](PRESERVATION_SUMMARY.json)。

新计算的校验值用于后续判断文件是否变化，不能单独证明原文件正确。未发现内容变化的结论只适用于本次实际计算或比对校验值的文件。文件系统未冻结，因此这份清单不是整个磁盘同一时刻的快照。

## 三、六个同级目录的处置

| 目录 | 当前处置 | 保留要点 |
| --- | --- | --- |
| `MSM-d5-training-clean` | 原位保留 | 仍是登记中的Git工作区，存在未纳入Git的实验文件；后续迁移需同时处理工作区登记 |
| `MSM-source-audit-request-20260803` | 原位保留 | 保留审计请求和两版预检材料 |
| `MSM-source-audit-result-20260803-v3` | 原位保留 | 保留审计结果、摘要和原校验文件 |
| `MSM-source-generation-output-64dfc08-20260803` | 原位保留 | 保留完整来源及其版本边界 |
| `MSM-source-generation-output-6737b44-20260803` | 原位保留 | 已完成来源与历史未完成运行不合并 |
| `MSM-source-generation-output-e7c438c-20260802` | 原位保留 | 已完成来源与历史未完成运行不合并 |

本次对这六个目录中纳入范围的35,845个普通文件均计算了校验值。Git管理信息、Python编译缓存、测试缓存和虚拟环境未纳入该清单，普通名为`cache`的目录仍保留。符号链接不跟随。

此前审计在训练工作区发现的32,485个非缓存忽略文件，属于“Git忽略文件”口径；本次盘点还包括工作区内其他普通文件，两项数字不能直接相减判断新增或丢失。目录中仍有绝对路径引用，未核对引用迁移前不改目录名称。

## 四、尚未完成的备份条件

盘点时系统盘可用空间为29.04 GiB，本次登记文件已达64.89 GiB。不能在当前分区再复制一份全量载荷，且同盘副本也不能应对系统盘故障。

下一步需要明确外置存储或备份服务的实际路径、可用容量和访问方式。建议为本次资料预留不少于100 GiB可用空间；复制前仍需按最终选择范围重新计算，尤其要加上四个外部仓库内部资料。

备份时分别处理以下内容：

1. 实验输出及其配置、清单、日志、模型、结果图，保持原运行目录边界。
2. 六个同级目录及工作区版本记录，不混入其他运行的数据。
3. 未提交的PDF、压缩包、网页资料和提取文本。
4. `paper/`下四个外部仓库及其本地未跟踪文件，不能只保存远端地址。
5. 本次登记、报告原件及其引用图片。

复制后需要核对源和目的文件数量、内容校验值及目录结构，并抽取文件进行恢复检查。当前只完成源端盘点，没有上述复制或恢复记录，暂不安排删除原数据。

## 五、历史失效路径

以下路径仍不存在：

```text
/tmp/msm-formal-r0-20260731-80e55eb/d6_strict_partial_450_b6289c5
```

[长期摘要目录](../../../research_modules/d2_data_association/docs/formal_r0_identity_causal_pack_summary_20260731/)仍在。本次在已扫描文件中按历史编号及压缩包命名查找，没有得到候选归档；检查不覆盖其他磁盘或存储服务，不能据此认定原始数据全部丢失。

后续应先查备份位置和旧归档记录。找到原始归档后，先确认版本、规模和校验值，再修改引用。无法找到时保留“原路径失效、摘要可查”的状态，不根据摘要补造原始输出。

## 六、后续安排

| 顺序 | 工作 | 当前状态 | 完成依据 |
| --- | --- | --- | --- |
| 1 | 源文件清单与重点校验 | 已完成 | 逐文件清单、分组表、196个来源文件比对结果 |
| 2 | Word与Markdown分别登记 | 已完成登记 | 26份Word、19份同名Markdown；两组版本差异单列 |
| 3 | 确认独立备份位置 | 等待位置 | 可访问的目标目录及容量检查 |
| 4 | 复制并验证恢复 | 未开始 | 源与副本校验一致，恢复抽查通过 |
| 5 | 明确多模态报告采用版本 | 待确认 | 指定汇报稿、参考稿与后续生成源 |
| 6 | 核查历史归档并决定迁移 | 未完成 | 归档证据、路径引用核查及迁移方案 |

目录清理应在备份和引用核查完成后单独决定。本次登记不包含删除授权，也未把所有历史试验判定为可复现。

## 七、工具与复核

- [只读盘点工具](../tools/prepare_preservation_register.py)在新目录生成清单，输出目录存在时停止。
- [测试文件](../tools/test_prepare_preservation_register.py)覆盖内容校验、读取期间变化、符号链接、空目录、目录边界和覆盖保护。
- 两份审计工具的14项单元测试通过，新增Python文件语法检查通过。
- 26份Word和19份同名Markdown的内容校验值均与原审计一致。本轮未重新渲染文档页面。

配套文档：[报告版本对照与使用规则](报告版本对照与使用规则_CN.md)。
