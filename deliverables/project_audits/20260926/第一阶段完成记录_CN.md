# 第一阶段完成记录

执行日期：2026年9月26日。负责人：main。

本阶段按已确认的范围固化接续依据，保存审计工具、文件清单、清理复查记录和工作线状态。前三批内容已分别提交、推送并核对远端提交号；本文与审计首页更新作为第四批提交，最终提交号及推送核验结果另列于交付消息，不回写本文。

## 一、完成范围

| 任务 | 完成内容 | 材料入口 |
| --- | --- | --- |
| T01：保存接续依据 | 已保存当前断点、后续计划及文件级快照，审计首页设有入口 | [接续计划](当前断点与后续工作计划_CN.md)、[状态快照](WORKSPACE_EVIDENCE_SNAPSHOT.json) |
| T02：明确提交范围 | 工具及测试、保留与清理登记、当前状态分别成批；只纳入审计目录 | [审计首页](../README.md)、下节提交记录 |
| T03：分组提交与推送 | 前三批逐次提交、推送，每次确认远端分支提交号与本地一致后再进入下一批 | 下节提交记录 |
| T04：建立工作线状态对照 | 分别登记原日期、版本、证据类型、停止位置及待确认事项，未改变模块原有结论 | [工作线状态对照](工作线状态对照_CN.md) |

历史计划和快照中的“尚未提交”“目前未执行”保留原样，表示本阶段执行前的状态。提交授权已在本阶段执行前另行确认，当前执行情况以本记录和Git历史为准。

本次未修改D1至D7模块、算法、原报告或实验数据，未执行仿真、训练、模型加载、数据迁移和删除。登记清单不是独立备份。

## 二、提交与远端核验

分支：`exp/d5-ideal-20target-registration`。远端：`origin`，地址为`git@github.com:robot-xyh/MSM.git`。

执行起点：`c10fb8bf827e2752cf72b03a21a46176598d1d08`。开始时已连接远端，确认本地与远端分支一致。

| 批次 | 内容 | 完整提交号 | 核验结果 |
| --- | --- | --- | --- |
| 1 | 两个审计工具及对应测试，共4个文件 | `26bf5908c56bf0521008273f1b2b977663996675` | 推送成功；远端分支提交号一致 |
| 2 | 保留登记6个文件、清理v1记录2个文件、清理v2记录3个文件，共11个文件 | `04b93c7455017901f8d74c87f492392346aef5e2` | 推送成功；远端分支提交号一致 |
| 3 | 当日快照、接续计划、工作线状态对照及审计首页，共4个文件 | `065e523e4362ae9f5532dffe68e4c0c0064d28b3` | 推送成功；远端分支提交号一致 |

每批均按明确路径暂存，并核对暂存文件列表。推送完成后使用以下命令读取服务器实际分支引用，与本地`HEAD`比较，没有仅依据本机远端跟踪引用判断：

```bash
git rev-parse HEAD
git ls-remote --exit-code origin refs/heads/exp/d5-ideal-20target-registration
```

清理v1保留为首轮工具结果，当前处置依据仍为[清理v2审计](../20260925_cleanup_review_v2/可清理数据审计_CN.md)。约6.8 MiB的`FILES.jsonl.gz`只含路径、大小及部分校验值，没有实验数据载荷。PDF、ZIP、外部嵌套仓库和缓存未纳入提交。

## 三、检查结果

| 检查项 | 本次结果 | 说明 |
| --- | --- | --- |
| 审计工具单元测试 | 23项全部通过 | 包含既有元数据审计工具；不是算法模块全量回归 |
| 新增Python文件语法 | 4个文件全部通过 | 使用`python3 -m py_compile`检查 |
| 原报告内容校验 | 26份Word、19份同名Markdown全部一致 | 与9月26日执行前快照比较，没有覆盖人工改稿 |
| 主要来源文件校验 | 196项全部一致 | 按快照中记录的原清单校验值重新读取文件，分组为150、3、27、16项 |
| 压缩清单完整性 | `gzip -t`通过，SHA-256与摘要一致 | 不重新生成历史盘点清单 |
| 清理工具版本 | SHA-256与清理v2记录一致 | 确认提交工具与登记工具对应 |
| Markdown本地链接 | 保留登记、清理报告、当日计划、状态对照及首页未发现失效链接 | 链接指向本机已有文件，不等于所有引用载荷均已推送 |
| 提交范围 | 前三批变动均位于`deliverables/project_audits/` | 未改动模块内部文档或其他业务文件 |
| 范围外未跟踪条目 | 207项，路径列表与开始时一致 | 保留原位；不等于对全部未跟踪内容完成逐字节复核 |

默认`git diff --check`将三个CSV原有的CRLF行末识别为空白告警。按CSV原格式执行`git -c core.whitespace=cr-at-eol diff --cached --check`后通过；未为消除告警改写历史CSV，也未修改仓库全局配置。其他新增文本使用默认空白检查通过。

本次核对的两个关键SHA-256为：

```text
FILES.jsonl.gz
f98b7fdd3c184d7b907fba4e8ba4952cead373d4d500b6feaf4cb9be4a740d2d

audit_cleanup_candidates.py
2d22ca8ff7fc8c1ab06ae44ff42c6236c3d5566429aece53adb64df1b3913622
```

文件和来源校验一致，只表明内容未变，不构成新的算法验证、设备验证或报告结论审定。

## 四、仍待处理的工作

1. 形成报告结论到原始表格、配置、图片及来源清单的逐项索引，标明统计口径和验证范围。
2. 整理中心配准、多模态报告的Word与Markdown差异，确认各版用途；确认前维持两版，不互相覆盖。
3. 明确独立备份位置、容量和范围，再安排复制、校验及恢复检查。目前未建立独立备份。
4. 缓存、重复ZIP及旧实验候选另行确认后处理。原清理记录仅为复查依据，执行前需要重新检查。
5. 继续查找历史失效路径的原始归档。未找到前保留“原路径失效、摘要可查”的状态，不补造原结果。

上述事项仍按[后续工作计划](当前断点与后续工作计划_CN.md)的依赖关系推进。本阶段没有关闭研究验证缺口，没有启动第二阶段以后的任务，也不据此给出项目总体完成率。
