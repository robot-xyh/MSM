# 野战防空反无人机体系技术补充

**来源**: 对 `field_air_defense_FINAL_REPORT_20260629.md` 的技术缺口补充  
**日期**: 2026年6月29日  
**用途**: 学术研究、体系工程论证  

---

## 一、研究边界

本文档用于补充主报告中缺失的体系工程细节，重点是探测融合、目标配准、资源分配、通信退化、分布式接管、数据接口和人机授权边界。

本文档不展开真实武器火控参数、毁伤参数、实装控制律或绕过人工授权的自主处置流程。所有不可逆处置在真实系统中必须保留人工授权接口；航迹不确定性过大时，系统只能推荐继续观测、请求确认或降级等待；分布式自主仅作为中心节点失效后的韧性保底能力，不作为正常状态下的首选控制方式。

---

## 二、系统级评价指标

主报告已有成本与效费比论证，但缺少统一技术指标体系。建议将指标分为六类。

| 指标类别 | 指标 | 含义 |
|----------|------|------|
| 探测 | detection_probability | 目标被至少一个传感器发现的概率 |
| 探测 | false_alarm_rate | 非目标被误报为目标的频率 |
| 探测 | missed_detection_rate | 目标在传感器覆盖区内未被发现的比例 |
| 跟踪 | track_continuity | 航迹持续稳定更新比例 |
| 跟踪 | track_rmse | 融合航迹与基准航迹之间的误差 |
| 跟踪 | id_switch_count | 多目标交叉或交接导致的ID切换次数 |
| 交接 | handover_success_rate | 不同探测系统之间保持同一目标ID的比例 |
| 分配 | duplicate_assignment_count | 多个资源重复分配同一目标的次数 |
| 分配 | unassigned_high_threat_count | 高威胁目标未被分配的次数 |
| 通信 | consensus_rounds | 分布式协商达到一致所需轮数 |
| 降级 | failover_time | 中心失效到备份/分布式接管的时间 |
| 降级 | degraded_completion_rate | 降级模式下任务完成率 |
| 人机协同 | human_override_count | 人工介入次数 |
| 安全约束 | constraint_violation_count | 禁区、友邻空域、最小间隔等约束违反次数 |

报告评估时不应只使用“命中率”或“成本”作为结论依据。多目标系统的关键问题往往是航迹ID稳定性、重复分配、目标漏分配、分布式接管时间和约束违反次数。

---

## 三、三层防御圈与复合探测网的融合逻辑

三层防御圈不应理解为三个彼此独立的火力区，而应理解为同一套目标航迹在不同距离、不同高度、不同置信度阶段下的资源接管关系。复合探测网负责生成统一目标航迹，三层防御圈只消费同一份融合态势。

融合流程可分为五个状态：

| 状态 | 触发条件 | 主要传感器 | 系统动作 |
|------|----------|------------|----------|
| 疑似目标 | 单一传感器发现异常 | 声学、雷达、ESM或光电 | 建立临时航迹，不分配拦截资源 |
| 确认目标 | 两类传感器一致，或单类传感器连续稳定跟踪 | 雷达+光电，声学+光电 | 生成全局目标ID，进入威胁排序 |
| 交战质量航迹 | 位置、速度、类别和协方差满足分配要求 | 雷达/光电/浮空器融合 | 允许进入资源分配队列 |
| 拦截中 | 已有拦截资源接管 | 中心节点、探测节点、资源节点 | 持续更新预测点和任务状态 |
| 效果评估 | 目标消失、坠落、热源消失或航迹中断 | 光电、雷达、声学 | 判定成功、失败或需要补位 |

统一航迹建议采用如下抽象字段：

```text
GlobalTrack
- global_track_id
- source_track_ids
- timestamp
- position_ned
- velocity_ned
- covariance
- classification
- confidence
- threat_score
- track_state
- assigned_resource_id
- handover_state
```

其中 `covariance` 表示定位不确定性，`track_state` 表示 tentative、confirmed、engageable、engaged、lost、dropped 等状态，`handover_state` 表示当前目标处于哪一个探测系统或防御圈的接管阶段。

---

## 四、时间同步与坐标基准

多源融合的前提是所有观测进入统一时间轴和统一坐标系。否则，目标配准、交接和分配都会产生系统性错误。

建议每条观测至少保留两个时间：

```text
measurement_timestamp：目标被传感器观测到的时刻
arrival_timestamp：观测到达融合节点的时刻
```

延迟应被分解记录：

```text
total_latency = sensor_latency
              + network_latency
              + queue_latency
              + processing_latency
```

坐标系建议：

| 坐标系 | 用途 |
|--------|------|
| WGS84 | 地理位置和地图显示 |
| ENU/NED | 局部融合跟踪 |
| sensor_frame | 单个传感器本体坐标 |
| platform_frame | 机载平台或地面平台坐标 |
| image_frame | 图像像素坐标 |

需要纳入工程校准的误差：

- 传感器安装位置误差。
- 传感器姿态外参误差。
- 平台姿态估计误差。
- 时间戳偏差。
- 坐标转换舍入误差。

---

## 五、传感器误差来源与观测质量

主报告描述了复合探测网，但还需要说明各传感器误差来源和观测质量管理方式。

| 传感器 | 主要误差来源 | 对系统的影响 |
|--------|--------------|--------------|
| 声学阵列 | 风、地形、背景噪声、多径反射 | 方位抖动、误警、类别混淆 |
| 地面雷达 | 地杂波、遮挡、低空视距限制、目标合并 | 位置误差、漏检、航迹合并 |
| 浮空器雷达 | 平台晃动、气象限制、通信中断 | 远距误差放大、航迹间歇 |
| 光电/红外 | 云雾、逆光、遮挡、背景复杂度 | 检测框抖动、分类混淆、视场边缘丢失 |
| 机载末端传感器 | 高速运动模糊、视场限制、目标遮挡 | 角度误差、目标丢失、重捕获延迟 |

建议每类传感器输出统一观测格式：

```text
SensorObservation
- sensor_id
- sensor_type
- timestamp
- measurement
- covariance
- classification_hint
- confidence
- raw_track_id
```

观测质量管理原则：

1. 低置信度观测只用于提示或维持航迹，不直接触发资源分配。
2. 单一传感器异常不应覆盖多源融合航迹。
3. 观测协方差应随距离、遮挡、平台姿态和环境条件变化。
4. 传感器失效或质量下降时，应降低其融合权重。

---

## 六、目标分类与数据治理

目标分类是资源分配的前置条件。分类体系应同时覆盖目标和非目标，避免把所有未知空中物体都归入威胁类别。

建议标签体系：

```text
target_class
- fixed_wing_uav
- multirotor_uav
- loitering_munition_like_object
- bird
- civilian_aircraft
- unknown
```

数据治理应覆盖：

- 不同高度、距离、姿态、速度的目标。
- 不同天气、光照、背景和遮挡。
- 鸟类、云层、地面车辆、民用航空器等负样本。
- 多目标交叉、编队和视场切换。
- 不同来源数据之间的域差异和标注一致性。

建议报告指标：

| 指标 | 含义 |
|------|------|
| precision | 被判定为目标的样本中真实目标比例 |
| recall | 真实目标被检出的比例 |
| f1_score | 精确率与召回率的综合指标 |
| confusion_matrix | 各类别之间的混淆关系 |
| false_positive_per_hour | 单位时间误报数量 |
| classification_latency | 分类输出延迟 |

对于高不确定性分类，系统应输出 `unknown`，而不是强行分类。`unknown` 目标可继续跟踪和请求确认，但不应自动升级为不可逆处置。

---

## 七、来袭目标定位方法

来袭目标定位应采用“多源观测、统一坐标、带不确定性输出”的原则。不同传感器提供的不是同一种信息：

| 传感器 | 主要观测量 | 定位作用 | 局限 |
|--------|------------|----------|------|
| 声学阵列 | 到达时间差、方位、声纹类别 | 低空粗定位和早期告警 | 距离精度有限，受风和噪声影响 |
| 地面雷达 | 距离、方位、俯仰、径向速度 | 三维位置和速度主来源 | 低空遮挡、近地杂波 |
| 浮空器雷达 | 大范围距离、方位、速度 | 远距预警和高空补盲 | 易受气象和平台稳定性影响 |
| 光电/红外 | 像素框、角度、类别、热源 | 目标确认、辅助关联、效果评估 | 单目距离不稳定，受天气影响 |

定位链路如下：

```text
本地传感器观测
-> 时间戳对齐
-> 坐标转换到统一NED坐标系
-> 观测协方差估计
-> 航迹预测
-> 数据关联
-> 滤波更新
-> 输出GlobalTrack
```

声学阵列通常先给出粗方位和类别置信度；雷达给出可用于融合的三维位置和速度；光电系统主要用于确认类别、降低误警和在目标交叉时辅助维持ID。单目光电不宜单独承担远距离三维定位任务，除非配合测距、双目/多目几何或已有雷达距离。

---

## 八、多目标配准与航迹管理

多目标配准的核心问题是：同一个真实目标会被多个传感器、多个视场、多个节点同时发现，系统必须判断这些局部观测是否属于同一个全局目标。

配准分三层：

1. **传感器内配准**：同一雷达或同一相机内部连续帧之间的目标关联。
2. **跨传感器配准**：雷达航迹、声学方位、光电检测框之间的关联。
3. **跨节点配准**：不同雷达站、天哨、地面站生成的局部航迹之间的关联。

推荐关联判据：

```text
时间门限：观测时间差在允许范围内
空间门限：观测位置落入预测协方差门限
速度门限：速度方向和大小与已有航迹一致
类别门限：声纹/图像/雷达特征分类不冲突
历史门限：连续多帧关联优先于单帧强匹配
```

多目标航迹状态机：

| 状态 | 含义 | 处理 |
|------|------|------|
| tentative | 新建但未确认 | 暂不分配拦截资源 |
| confirmed | 多帧稳定或多源一致 | 进入威胁排序 |
| engageable | 定位精度和类别置信度满足要求 | 可参与资源分配 |
| engaged | 已有资源接管 | 持续更新和防止重复分配 |
| lost | 短时丢失 | 用预测维持，限制新分配 |
| dropped | 超时未恢复 | 释放资源并归档 |

算法路线建议：

| 算法 | 优点 | 缺点 | 推荐用途 |
|------|------|------|----------|
| 最近邻 | 简单、可解释 | 容易在交叉航迹中发生ID切换 | 基线方案 |
| GNN/Hungarian | 全局一对一匹配，工程成熟 | 依赖合理代价矩阵 | 多目标主线 |
| JPDA | 能处理一个观测对应多个假设 | 计算复杂度较高 | 密集目标研究 |
| MHT | 保留多条历史假设 | 资源消耗大 | 小规模高精度研究 |
| IMM-EKF/UKF | 可处理不同机动模型 | 参数和模型切换需调试 | 机动目标跟踪 |

当目标交叉、编队密集或视场切换时，系统应记录 `id_switch_count`。若同一目标在融合系统中频繁更换ID，会导致重复分配、漏分配或补位错误，因此ID稳定性应作为多目标系统的核心指标，而不只统计命中率。

---

## 九、多探测系统交接与不同视场目标关联

探测系统交接不是简单的“雷达交给光电”或“外圈交给内圈”，而是航迹主导权的转移。建议采用 `handover_token` 机制：每个 `GlobalTrack` 同一时刻只有一个主传感器负责主更新，其他传感器作为辅助更新源。

典型交接链路：

```text
浮空器雷达远距发现
-> 地面雷达接力稳定三维航迹
-> 声学阵列低空确认方向和类别
-> 光电系统获取图像确认
-> 资源节点末端传感器接管
-> 光电/雷达进行效果评估
```

不同视场下同一目标的关联方法：

| 情况 | 关联方法 |
|------|----------|
| 广域雷达到窄视场光电 | 用雷达预测位置控制光电指向，再用角度门限关联 |
| 广角光电到长焦光电 | 先保持同一平台姿态解算，再比较目标角速度和外观特征 |
| 上级视场到下级视场 | 使用目标方位、预测位置、外观类别和接管时间窗联合判断 |
| 两个雷达站重叠覆盖 | 使用位置协方差、速度一致性和历史航迹连续性判断 |

交接失败时不应立即删除目标，而应进入短时 `lost` 状态，由上一主传感器预测维持；若新传感器在时间窗内重新捕获，则恢复原 `global_track_id`，避免因视场切换产生新目标ID。

---

## 十、分距离拦截与5对5目标关联方案

本节给出中心节点存在时的分距离处置逻辑。这里的“拦截”指体系工程中的资源接管和任务分配，不展开真实火控参数、毁伤参数或末端控制细节。

### 10.1 分距离信息质量与接管关系

不同距离下，系统掌握的信息质量不同。应按航迹质量逐步接管，而不是让资源节点直接依赖单一传感器。

| 距离阶段 | 主要信息源 | 航迹质量 | 系统动作 |
|----------|------------|----------|----------|
| 远距预警段 | 浮空器雷达、地面主雷达 | 位置误差较大，速度方向可用，分类置信度较低 | 建立 tentative/confirmed 航迹，预测来袭方向，不分配近距资源 |
| 中距跟踪段 | 地面雷达、角站雷达、空中侦察无人机 | 三维位置和速度稳定，协方差下降 | 形成 engageable 航迹，启动资源可行性计算 |
| 近距交接段 | 空中侦察无人机光电、地面光电、声学阵列 | 类别确认增强，目标ID稳定性成为重点 | 完成目标-资源初始分配，准备传感器交接 |
| 末端确认段 | 拦截资源机载相机、空中侦察无人机、地面雷达辅助 | 局部视场内目标精细，视场外目标依赖中心航迹 | 视场内使用视觉确认，视场外继续按中心航迹/侦察机航迹引导接近 |
| 效果评估段 | 空中侦察无人机、地面雷达、光电/声学 | 判断航迹是否消失、分裂或继续存在 | 标记 completed、failed、lost 或需要补位 |

雷达定位精度不应写成固定常数，而应写成随距离、目标高度、杂波、遮挡和目标数量变化的 `covariance`。工程上可分为三级：

| 航迹质量等级 | 含义 | 可执行动作 |
|--------------|------|------------|
| coarse_track | 只知道大致区域和方向 | 预警、扇区指向、资源预备 |
| stable_track | 位置和速度连续稳定 | 资源分配、航迹预测、接近点生成 |
| handover_track | 位置、速度、类别、ID均较稳定 | 允许交接给光电或机载相机确认 |

### 10.2 雷达定位延迟下的接近逻辑

中心节点不应把雷达最新点位直接当作目标当前位置，而应维护延迟补偿后的预测航迹：

```text
radar_measurement_time = 雷达实际观测时刻
c2_receive_time = 中心节点收到观测时刻
latency = c2_receive_time - radar_measurement_time
predicted_target_state = track_state_at_measurement_time + velocity_model * latency
```

资源节点接近目标时，建议使用“预测接近区”而不是单点目标：

```text
PredictedInterceptVolume
- center_position
- velocity_vector
- covariance_ellipsoid
- valid_until
- primary_sensor
- backup_sensor
```

这样做的目的有三点：

1. 雷达延迟或通信延迟不会让资源节点追逐过期点位。
2. 航迹协方差越大，接近区越大，系统越倾向于请求侦察无人机或光电系统补充确认。
3. 资源节点进入接近区后，再由机载相机、空中侦察无人机或地面光电完成目标确认。

### 10.3 5个入侵目标与5个拦截资源的中心化分配

设来袭目标为 `T1-T5`，可用拦截资源为 `R1-R5`，空中侦察无人机为 `S1`，中心节点维护全局航迹表：

```text
TrackTable = [T1, T2, T3, T4, T5]
ResourceTable = [R1, R2, R3, R4, R5]
ScoutTrack = S1
```

中心节点每个分配周期执行：

1. 剔除 `tentative`、`lost`、`dropped` 或低置信度目标。
2. 剔除不可用、越界、故障或已被人工锁定的资源。
3. 对每个 `Ri-Tj` 组合计算代价。
4. 对不可行组合赋予不可分配状态。
5. 使用 Hungarian 或最小费用流生成一对一分配。
6. 为高威胁目标保留备份资源或补位规则。
7. 发布 `AssignmentPlan`，并记录版本号。

代价函数建议保持抽象：

```text
assignment_cost = 接近窗口代价
                + 航迹不确定性惩罚
                + 目标威胁权重
                + 资源状态惩罚
                + 视场确认难度
                + 资源间冲突风险
```

5对5情况下默认是一对一分配，但需要考虑两类异常：

| 异常 | 处理 |
|------|------|
| 某个目标突然丢失 | 资源进入预测接近区等待，中心节点请求侦察无人机/雷达/声学补充确认 |
| 某个资源未能确认目标 | 不立即改追其他目标，先进入局部搜索/等待确认状态，避免造成重复分配 |

### 10.4 视场内目标与视场外目标的关联

当5架资源节点接近目标群时，可能出现部分目标在机载相机视场内、部分目标不在视场内。中心节点应把目标分成三类：

| 类型 | 含义 | 处理 |
|------|------|------|
| in_fov_confirmed | 机载相机看到目标，且与中心航迹匹配 | 资源节点继续跟踪该 `global_track_id` |
| in_fov_unmatched | 机载相机看到目标，但无法匹配现有航迹 | 上报新视觉观测，由中心节点尝试关联或新建 tentative 航迹 |
| out_of_fov_tracked | 机载相机未看到目标，但中心航迹仍稳定 | 资源节点继续飞向预测接近区，同时请求侦察无人机或地面雷达更新 |

机载相机观测与中心航迹关联时，至少使用四类约束：

```text
角度一致性：目标是否落在预测视线附近
时间一致性：视觉观测时间是否接近航迹预测时间
运动一致性：图像角速度是否符合目标相对运动趋势
类别一致性：视觉类别是否与融合分类冲突
```

若 `R2` 的相机看到一个目标，但该目标同时也落在 `R3` 的预测视线附近，中心节点不应让两架资源同时抢占同一目标，而应按 `AssignmentPlan` 的版本号和代价优先级保留一个主资源，另一个资源进入备份、等待或重新分配状态。

### 10.5 地面雷达、声学、机载相机和侦察无人机的协同关联

空中侦察无人机 `S1` 的作用不是替代中心节点，而是提供上视角/侧视角补盲，尤其适合处理视场外目标、低空遮挡目标和多目标交叉。

协同关联逻辑：

```text
地面雷达：提供三维位置、速度、协方差
声学阵列：提供低空方位、声纹类别和粗略方向确认
侦察无人机：提供广域光电检测、类别确认、交叉航迹ID维持
资源节点相机：提供局部视场内目标确认
中心节点：统一关联、统一分配、统一版本控制
```

当某目标不在资源节点相机视场中时，中心节点按优先级使用其他信息源：

1. 使用地面雷达预测目标当前位置和协方差。
2. 使用侦察无人机确认该目标是否仍存在、是否与邻近目标交叉。
3. 使用声学阵列确认低空目标方位和类别一致性。
4. 若仍无法确认，则把目标状态降为 `lost` 或 `tentative`，暂停不可逆升级。

### 10.6 空中侦察无人机参与交接

空中侦察无人机适合作为“中继观察者”和“ID稳定器”。它应维护自己的局部航迹，但不直接覆盖中心节点全局航迹：

```text
ScoutObservation
- scout_id
- timestamp
- bbox_or_bearing
- classification_hint
- confidence
- candidate_global_track_ids
```

侦察无人机参与三类交接：

| 交接类型 | 作用 |
|----------|------|
| 雷达到侦察无人机 | 雷达提供预测位置，侦察无人机转向确认目标类别和队形 |
| 侦察无人机到资源节点 | 侦察无人机给出目标方位和候选ID，资源节点尝试在局部视场确认 |
| 资源节点到侦察无人机 | 资源节点丢失目标时，侦察无人机维持该目标ID并辅助重捕获 |

侦察无人机和资源节点都看到同一目标时，中心节点应以 `global_track_id` 为准，不以局部相机检测框编号为准。局部检测框只能作为全局航迹的观测源，不能单独改变任务分配。

### 10.7 分距离方案的状态闭环

完整闭环如下：

```text
远距雷达/浮空器发现
-> 中心节点建立GlobalTrack
-> 中距地面雷达稳定航迹
-> 空中侦察无人机辅助确认类别和队形
-> 中心节点对T1-T5与R1-R5做一对一分配
-> R1-R5飞向各自PredictedInterceptVolume
-> 视场内目标由机载相机确认
-> 视场外目标由雷达/侦察无人机/声学继续维持
-> 中心节点处理ID冲突、重复分配和重分配
-> 任务完成、失败、丢失或补位
```

该方案的关键不是让每架资源节点独立判断“看到什么就追什么”，而是让所有局部观测回到中心节点的 `GlobalTrack` 和 `AssignmentPlan`。这样可以降低多目标密集来袭时的ID交换、重复分配和漏分配风险。

---

## 十一、拦截资源状态机与目标分配

本文中的“武器目标分配”在系统工程上建议表述为“拦截资源-目标分配”。其输入是已确认目标集合和可用拦截资源集合，输出是任务配对、优先级和备份关系。该过程应保留人工授权接口，尤其在真实环境中不得由模型绕过指挥员直接执行不可逆动作。

资源状态机：

| 状态 | 含义 |
|------|------|
| available | 可分配 |
| reserved | 已预留但未执行 |
| assigned | 已分配任务 |
| enroute | 正在执行任务 |
| terminal | 进入末端阶段或效果确认阶段 |
| completed | 任务完成 |
| failed | 任务失败 |
| returning | 返回或退出 |
| unavailable | 故障、耗尽、越界或人工锁定 |

分配输入：

```text
TargetSet = confirmed / engageable GlobalTrack
ResourceSet = 可用拦截资源、剩余数量、状态、覆盖范围、任务占用
ConstraintSet = 禁区、友邻空域、最小间隔、人工授权状态
```

威胁评分可按抽象因素计算：

```text
threat_score = 目标类型权重
             + 接近保护区域程度
             + 预计到达时间紧迫性
             + 航迹置信度
             + 饱和攻击中的编队位置
```

资源分配代价可按抽象因素计算：

```text
assignment_cost = 拦截窗口代价
                + 资源消耗代价
                + 航迹不确定性惩罚
                + 重复分配惩罚
                + 友邻冲突惩罚
                + 备用资源占用惩罚
```

分配算法对比：

| 算法 | 优点 | 缺点 | 推荐用途 |
|------|------|------|----------|
| 规则/最近邻 | 简单，便于调试 | 容易局部最优 | 基线方案 |
| Hungarian | 匹配质量稳定 | 需要中心节点和代价矩阵 | 中心化主线 |
| 最小费用最大流 | 支持容量和多约束 | 约束表达复杂 | 多资源扩展 |
| 拍卖算法 | 通信量较小，适合分布式 | 收敛和冲突处理需评估 | 降级模式 |
| CBBA | 适合多智能体任务规划 | 实现复杂 | 科研对照 |

默认分配原则：

1. 高威胁目标优先获得拦截资源。
2. 低成本、低附带风险资源优先使用。
3. 一个目标默认只分配一个主资源，另可保留一个备份资源。
4. 同一资源同一时刻只执行一个主任务。
5. 航迹不确定性过大时先请求进一步确认，不直接进入不可逆处置。

---

## 十二、通信网络与退化机制

主报告描述了Mesh、备份链路和地面互联，但还需要说明通信退化时系统如何保持态势一致性。

建议关注变量：

| 变量 | 含义 |
|------|------|
| link_latency | 单条链路通信延迟 |
| packet_loss_rate | 丢包率 |
| bandwidth_limit | 单位时间可传输消息量 |
| node_dropout_rate | 节点掉线概率 |
| network_partition | 网络分区事件 |
| recovery_time | 链路恢复时间 |
| clock_sync_error | 节点间时钟偏差 |

退化场景：

1. 固定延迟：所有消息增加固定延迟。
2. 随机延迟：消息延迟不稳定。
3. 随机丢包：部分航迹或资源状态丢失。
4. 局部分区：节点被分成多个互不通信子网。
5. 节点失效：探测节点、融合节点或资源节点下线。
6. 恢复合并：分区恢复后航迹表和任务表重新合并。

通信退化时应重点记录：

```text
message_delivery_rate
mean_latency
p95_latency
partition_duration
consensus_rounds
assignment_conflict_count
track_divergence_count
```

---

## 十三、中心节点失效后的切换机制

中心节点失效不应由单一“掉线”事件触发，而应由健康监测状态机判断。建议每个节点维护 `C2Health`：

```text
C2Health
- heartbeat_age
- last_track_update_age
- assignment_update_age
- command_ack_rate
- clock_sync_error
- link_quality
- health_state
```

`health_state` 可分为 normal、degraded、suspect、failed 四级。只有当心跳中断、航迹更新中断、任务分配更新中断同时满足超时条件时，才进入 failed，避免短时链路抖动导致全网误切换。

切换流程：

```text
中心节点normal：所有节点接收中心融合航迹和分配结果
中心节点degraded：分布式节点开始缓存本地观测和邻居观测，准备接管
中心节点suspect：候选备份节点发起接管投票，冻结非必要重分配
中心节点failed：备份节点或分布式共识层接管航迹融合与任务分配
中心节点恢复：进入双轨校验，不立即夺回控制权
```

多候选备份节点同时接管时，需要 leader election 抽象规则：

```text
leader_priority = 节点角色优先级
                + 航迹表新鲜度
                + 可用链路数量
                + 算力/供电健康状态
                - 当前任务冲突数量
```

建议所有航迹表和任务表维护版本号：

```text
track_table_epoch
assignment_plan_epoch
leader_term
```

中心恢复后不应直接覆盖分布式态势，而应执行航迹合并、任务状态合并和双轨校验，再逐步恢复中心主控。

---

## 十四、分布式节点的自主决策边界

分布式节点接管后，其目标不是复制完整指挥所，而是在信息不完整条件下维持局部防御能力。自主决策应分为三类：

| 决策类型 | 可自动执行 | 需要人工确认 | 禁止自动越权 |
|----------|------------|--------------|--------------|
| 态势维护 | 本地检测、航迹预测、邻居观测融合 | 航迹身份冲突 | 篡改其他节点历史记录 |
| 任务协商 | 候选目标排序、资源占用声明、冲突消解 | 高不确定性目标处置 | 绕过授权执行不可逆动作 |
| 生存控制 | 返航、待机、通信重连、避碰 | 改变任务区域 | 进入禁区或友邻空域 |

分布式自主决策的基本原则：

1. **保守确认**：单节点单帧观测只建立 tentative 航迹，不直接进入处置。
2. **局部一致**：相邻节点对目标ID、类别和任务占用达成一致后再执行。
3. **冲突优先**：如果任务分配与避碰、禁区或友邻识别冲突，优先中止或等待。
4. **可追溯**：每次分布式分配必须记录参与节点、输入航迹、分配版本和决策原因。
5. **人机边界**：涉及真实不可逆处置时，应保留人工授权或预先设定的约束。

---

## 十五、分布式航迹融合与任务协商

中心节点失效后，各分布式节点维护局部航迹表：

```text
LocalTrackTable
- local_track_id
- estimated_global_id
- source_node_id
- timestamp
- position_ned
- velocity_ned
- covariance
- classification
- confidence
- observed_by_neighbors
```

节点之间交换的不是原始视频或全量雷达数据，而是压缩后的航迹摘要和资源状态：

```text
TrackSummary
- estimated_global_id
- timestamp
- position_ned
- velocity_ned
- covariance
- classification
- confidence

ResourceSummary
- node_id
- resource_id
- availability
- current_task_id
- energy_state
- local_constraints
```

分布式任务协商可采用拍卖式流程：

```text
每个节点根据本地航迹计算目标优先级
每个可用资源对候选目标计算本地代价
节点广播自身出价和任务占用声明
邻居节点比较出价，低代价者获得临时任务权
若出现冲突，按航迹版本、节点优先级和代价差消解
若多轮后未收敛，保留原任务或进入待命
```

这种方式的优势是通信量小、可局部收敛；缺点是全局最优性不如中心节点。因此分布式模式应定位为“中心失效后的韧性保底能力”，而不是正常状态下的首选模式。

---

## 十六、C2数据总线与模块接口

为支撑复合探测网、多层防御圈和分布式降级运行，C2系统建议采用统一数据总线，而不是由各传感器直接驱动各拦截资源。

核心数据对象：

```text
SensorObservation
- sensor_id
- sensor_type
- timestamp
- measurement
- covariance
- classification_hint
- confidence

GlobalTrack
- global_track_id
- source_track_ids
- timestamp
- position_ned
- velocity_ned
- covariance
- classification
- confidence
- threat_score
- track_state

ResourceState
- resource_id
- resource_type
- availability
- current_task_id
- coverage_zone
- remaining_capacity
- health_state

AssignmentPlan
- plan_id
- timestamp
- target_track_id
- primary_resource_id
- backup_resource_id
- priority
- confidence
- human_authorization_state
```

C2处理链路建议为：

```text
SensorObservation
-> 数据清洗与时间同步
-> 坐标统一
-> 局部航迹生成
-> 全局航迹融合
-> 威胁评分
-> 资源可行性筛选
-> 资源-目标分配
-> 人工确认/规则确认
-> 任务发布
-> 效果评估
```

该结构的优点是传感器、融合算法、资源分配算法和显示界面之间解耦。后续替换传感器、分配算法或通信链路时，不需要重写全系统。

---

## 十七、人机协同与授权状态

为避免自动系统在高不确定性条件下做出不可逆决策，建议所有任务计划维护 `human_authorization_state`：

| 状态 | 含义 | 系统行为 |
|------|------|----------|
| observe_only | 仅观察 | 只跟踪和显示，不分配处置资源 |
| recommend | 系统推荐 | 生成分配建议，等待确认 |
| authorized | 已授权 | 按规则发布任务 |
| hold | 暂停 | 保持航迹，不继续升级 |
| abort | 中止 | 释放资源，记录原因 |

授权状态应参与资源分配，而不是只在界面层显示：

```text
if human_authorization_state in [observe_only, recommend, hold]:
    assignment_plan.can_execute = false
if track_confidence < threshold:
    human_authorization_state = recommend
if constraint_violation_detected:
    human_authorization_state = abort
```

任何真实系统讨论中，不应让模型绕过授权状态直接执行不可逆动作。

---

## 十八、运行记录与可追溯性

系统应保留运行记录，以便事后审计、误报分析、任务复盘和责任追踪。

建议记录字段：

```text
TrackRecord
- timestamp
- global_track_id
- source_track_ids
- state
- covariance
- track_state

AssignmentRecord
- timestamp
- plan_id
- target_track_id
- resource_id
- algorithm
- assignment_cost
- authorization_state

EventRecord
- timestamp
- event_type
- affected_nodes
- reason
- recovery_action
```

记录系统至少需要支持：

1. 按时间轴查看传感器观测和融合航迹。
2. 标记每次ID切换和交接失败。
3. 标记每次分配、重分配和人工授权状态变化。
4. 输出指标表和态势摘要。

---

## 十九、成本模型与不确定性分析

主报告已有静态成本估算，但评审时还需要不确定性分析。

建议补充变量：

| 成本变量 | 含义 |
|----------|------|
| sensor_attrition_rate | 探测节点损耗率 |
| resource_consumption_rate | 拦截资源消耗率 |
| maintenance_cost | 维护和备件成本 |
| training_cost | 人员训练和演练成本 |
| false_alarm_cost | 误警导致的资源调动成本 |
| inventory_depth | 备件和消耗品库存深度 |
| replacement_time | 损耗节点补充时间 |

敏感性分析方法：

```text
固定其他变量
逐一改变单个成本变量
观察总成本、任务完成率和降级生存能力变化
找出成本-效能最敏感项
```

建议输出：

- 基准成本。
- 乐观/中性/悲观三档成本。
- 关键部件损耗率变化下的系统效能。
- 误警率变化导致的资源消耗变化。

---

## 二十、参考资料与可信度标注

主报告末尾数据来源混杂，建议按可信度分组：

| 类型 | 示例 | 使用方式 |
|------|------|----------|
| 官方资料 | 政府、军方、监管机构公开文件 | 用于法规、制度、公开装备约束 |
| 学术论文 | 期刊、会议、综述论文 | 用于算法、传感器模型、评估方法 |
| 企业资料 | 产品白皮书、规格书 | 用于概念参考，需标注商业宣传偏差 |
| 新闻报道 | 战场统计、媒体报道 | 用于背景，不作为精确技术参数 |
| 系统运行记录 | 内部测试、演练、复盘记录 | 用于方案内部验证和改进 |

引用建议：

1. 所有关键数字给出来源和日期。
2. 未经独立验证的战场数据标注“不确定”。
3. 产品宣传中的性能指标不得直接作为系统真实性能。
4. 成本、命中率、损耗率应给出范围或敏感性分析，而不是单点结论。
