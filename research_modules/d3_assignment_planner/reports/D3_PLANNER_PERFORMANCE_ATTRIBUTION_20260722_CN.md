# D3 规划热路径冻结输入归因

## 结论

本次使用同一份匿名、可复现的 200×200 输入，分离 D3 成本矩阵、候选图、Hungarian 求解、迟滞、计划载荷、身份发布和离线证据构造。默认路径、身份重复计算参考路径和关闭离线证据参考路径的绑定哈希、计划版本及规范业务哈希一致。最新发布执行签名始终来自规划器内部缓存，不使用调用方 previous plan 代替。参考路径只用于归因，不是运行时替代方案。

D3 暖启动耗时主要由候选边计划证据、上一计划帧的迟滞重评分和匿名离线证据构成。Hungarian 在当前稀疏候选图下不是唯一成本来源。集成 10 秒 episode 的累计时间还受 D3 调用次数和上游输入形状影响，不能用单次墙钟直接解释；main 需在隔离工作树中对同一 episode 再测。

## 冻结输入

- fixture schema：`d3_reproducible_assignment_fixture_v1`
- seed：`42000`
- 输入 SHA-256：`c7c86f22252add5a6e201577ec99baa63050e56d00898e66d514ab3c0c46c7ff`
- 目标数：200
- 资源数：200
- 每目标候选边上限：32
- 重复次数：3
- 在线真值：未使用。目标标识是匿名 GlobalTrack 代理，不是仿真真实编号。

## 热点边界

| 阶段 | 首帧结构操作数 | 上一计划帧结构操作数 | 边界 |
| --- | ---: | ---: | --- |
| 全量目标资源对 | 40000 | 40000 | 向量化规则代价计算 |
| 候选边 | 6400 | 6400 | 稀疏候选图与计划边证据 |
| 候选连通分量 | 1 | 1 | 分量内 Hungarian |
| Hungarian 准备矩阵单元 | 80000 | 80000 | 局部代价矩阵加未分配虚拟列 |
| 计划边哈希条目 | 6400 | 6400 | `cost_breakdowns_by_edge` 规范哈希 |
| 迟滞候选边访问 | 0 | 6400 | 去除搜索整形后的当前目标重评分 |
| 迟滞绑定重评分 | 0 | 400 | 旧计划与候选计划绑定 |
| 匿名证据矩阵复制单元 | 80000 | 80000 | 规则矩阵和实际求解矩阵的只读快照 |
| 匿名 breakdown 单元访问 | 40000 | 40000 | 保留共享模板的匿名化遍历 |
| 匿名 breakdown 实际净化数 | 6401 | 6401 | 按对象身份复用净化结果 |

## 墙钟结果

墙钟仅写入本基准报告，没有进入 `AssignmentPlan.metadata`、`plan_id` 或运行时控制合同。各阶段为包含式边界，不应相加解释总耗时。

| 路径 | 帧 | 端到端/ms | 成本矩阵/ms | Hungarian/ms | 计划边证据/ms | 迟滞/ms | 身份固化/ms | 发布/ms | 离线证据/ms |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 默认 | 首帧 | 304.081 | 58.782 | 3.603 | 75.750 | 0.005 | 1.301 | 0.017 | 62.448 |
| 默认 | 上一计划帧 | 309.180 | 60.027 | 3.659 | 74.684 | 31.093 | 3.045 | 0.163 | 71.874 |
| 身份重复计算参考 | 上一计划帧 | 369.201 | 58.888 | 3.839 | 78.732 | 30.978 | 14.688 | 6.073 | 75.116 |
| 关闭离线证据参考 | 上一计划帧 | 224.900 | 59.135 | 4.284 | 75.074 | 29.347 | 2.903 | 0.171 | 0.013 |

## 语义校验

- 默认首帧绑定 SHA-256：`794a9eef1d94c26fd599dde2a8999e7d9a0726776f96b0daa4edb67a6f3a9039`
- 默认首帧业务 SHA-256：`79e2ff6fc1d3f516fa0a5ee30f99508e52eb25974f04f8a979c954e02bf1e0ad`
- 默认刷新帧绑定 SHA-256：`794a9eef1d94c26fd599dde2a8999e7d9a0726776f96b0daa4edb67a6f3a9039`
- 默认刷新帧业务 SHA-256：`8aee3130a60cb6581214837ca0cc79a94c139bd4bb6c8b23e36dec1f12c5e030`
- 三条路径绑定一致：`True`
- 三条路径计划版本一致：`True`
- 三条路径规范业务哈希一致：`True`
- 刷新帧复用原 `plan_id`：`True`
- 最新发布签名来源：`planner_owned_cache`
- 调用方 previous 签名充当 latest：`False`
- 规则代价、Hungarian、迟滞和 D5/D7 binding 均未修改。

## main 复测要求

main 应在最终候选提交建立 detached clean worktree，使用 seed 42000 的同一 2.2 秒与 10 秒配置复跑，再扩展 seed 42001、42002。复测需同时核对 D3 调用次数、每次目标/资源/候选边数量、绑定哈希、计划版本、运行时 ACK、在线真值使用次数和累计阶段墙钟。只有冻结输入的单次成本与调用密度都能解释累计差异时，才能判断 2.484 秒到约 3.348 秒的变化来自 D3 代码或 episode 调度。
