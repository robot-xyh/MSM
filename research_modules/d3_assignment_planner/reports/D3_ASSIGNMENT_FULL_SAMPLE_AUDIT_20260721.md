# D3 正式分配数据全样本准入审计

验证日期：2026-07-21

## 结论

正式数据的逐文件、逐 episode、逐决策帧结构审计状态为 `complete`。审计发现 0 项结构或绑定违规。
当前总体状态为 `partial`。数据没有当前计划 owner、当前计划 version、真实运行应答、结果归因和同 seed 配对 shadow 记录，不能据此启动 PPO、开放 assist 或改变在线权限。规则代价和需求槽匈牙利继续作为默认路径。

## 数据规模

| 口径 | 训练 | 验证 | 测试 | 合计 |
| --- | ---: | ---: | ---: | ---: |
| 规范 episode 身份 | 60 | 20 | 20 | 100 |
| 实际场景 episode | 540 | 180 | 180 | 900 |
| 决策帧/样本 | 962 | 320 | 322 | 1604 |
| 候选边/动作标签 | 2229182 | 721445 | 708188 | 3658815 |
| 规则选中动作 | 71425 | 23147 | 22732 | 117304 |

共 900 个实际 episode、1604 个决策样本、3658815 条候选边和 117304 条规则选中动作。60/20/20 是 100 个数值 seed 对应的规范 episode 身份切分；实际 episode 为 540/180/180，决策帧为 962/320/322。

## 审计范围

- 校验 manifest、frame 数据、训练 seed 注册表、共享切分注册表、生成摘要、episode 进度和批量导出摘要的 SHA256。
- 逐帧校验 schema、有限数值、候选边和动作维度、索引、容量、需求槽、匿名 token、切分和前序版本单调性。
- 逐 episode 校验导出帧计数、有限状态、脏工作树标记和在线真值使用计数。
- 审计只读运行；正式源文件审计前后哈希一致。

## 结果

- 在线真值使用：0。
- 脏 episode：0。
- 非法 global_track_id 字段：0。
- 前序计划版本回退：0。
- 数据内容哈希：`954f3e96d563412644ec88d1b621e2a58c781af8af99de79b859d22079fc1867`。

正式生成过程中有部分规划 tick 因权威代次围栏或计划与成本帧不匹配而没有导出学习帧。进度表记录 194 个未导出帧，原因分布为 `{"authority_generation_fence_has_no_cost_frame": 120, "published_plan_has_no_matching_cost_frame": 74}`。审计按缺失原因保留该事实，没有用上一帧替代。

## 证据边界

当前数据只保存匿名 ordinal token 和 `previous_plan_version`。它可以证明样本内部索引、需求槽、容量和前序版本序列合法，不能证明当前计划 owner、当前计划 version 或运行时 stale 拒绝已经发生。离线 reward 分量来自规则教师，不能替代真实运行结果或可归因回报。

后续准入仍需真实 runtime applied ACK、结果归因、因果或反事实 reward，以及同 seed 的规则路径与学习 shadow 非退化证据。上述证据闭合前，PPO、assist、在线权限和模型权重写入保持关闭。
