# D5 三维长时性能复核

## 结论
同输入基线 `c0460e0` 对照中，终端重放由 4.133 秒降至 2.776 秒，加速 1.489 倍；主动视觉每轮由 37.431 毫秒降至 25.918 毫秒，加速 1.444 倍。


在线日志重放 116 次终端关联，记录与重放核心哈希一致，规则绑定语义保持。3 次重放中位墙钟为 2.776 秒。
主动视觉按 205 条中心航迹、208 台相机和 199 个分配引用运行，中位每轮 25.918 毫秒。学习辅助未授权，输出继续使用确定性规则路径。

## 长时根因

clean 对照中，主动视觉单次成本在 2.2 秒和 10 秒阶段分别为 53.127 和 53.261 毫秒，总成本增长主要来自稳态调用次数。终端关联单次成本由 13.152 毫秒增至 32.143 毫秒；每次调用的视觉检测均值由 3.696 增至 21.491，后段每帧约有 26 至 39 个图节点。没有发现 tracklet 历史或稀疏候选边随仿真时间无界累积。

收敛项包括快照派生索引、中心投影距离矩阵复用、D2 DTO 内容指纹缓存和匿名 tracklet 序号集合。内容指纹覆盖适配实际读取的状态、协方差、航迹时间戳、版本和标识，episode reset 清空缓存；相机观测双时间戳仍按原路径处理。

## 终端关联

- 调用数：116
- 局部航迹总数：2493
- 稀疏图边总数：135
- 绑定状态：{"ambiguous": 36, "bound": 1938, "unbound": 384}
- 记录哈希：`7f212c56a6b477f58a37a692f7f3f2c227509dd1f485100f2b1c1a61435254e4`
- 重放哈希：`7f212c56a6b477f58a37a692f7f3f2c227509dd1f485100f2b1c1a61435254e4`
- 在线真值使用：0
- 中心航迹编号改写：0

## 主动视觉

- 仿真轮数：20
- 决策数：4160
- 意图分布：{"observe_target": 3980, "search_sector": 180}
- 决策哈希：`a9d3d3f299ecbe8fd6c3d33cdbbe7803e8959b9055faf8b38fc2b62ff7ff58f6`

## 发布载荷

`modules.d5.active_vision` 共 93 条、8.273 MB。
`modules.d5.terminal_association` 共 116 条、0.779 MB。
两个 `module.d5` 阶段计时在发布对象构造和总线序列化之前结束。因此上述载荷不是 D5 内部阶段超线性增长的来源；它会进入 main 总线和日志写出成本，压缩或降采样属于 main 的消息合同与可观测性取舍，本模块未修改。

## 证据边界

本复核读取在线消息，不读取离线真值、目标实体编号或仿真对象编号。终端关联为真实日志重放；主动视觉为同规模确定性合成负载。结果不代表 AirSim 实时性，也不授予图模型或主动视觉学习策略在线权限。
