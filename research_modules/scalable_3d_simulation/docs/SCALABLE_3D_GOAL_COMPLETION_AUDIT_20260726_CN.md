# 200 对 200 三维体系目标完成度审计

## 审计结论

当前专项分支已经具备三维规则闭环、D1-D7 统一总线、匿名视觉投影、学习数据管线、模型
影子运行、正式实验分片和 D6 失败关闭审计。完整目标尚未完成。主要缺口集中在学习模型
预准入、正式多随机种子矩阵、200 对 200 实时性、模型制品交付和最终综合报告。

截至 2026-07-26，当前基线 HEAD 为 `1815673`，分支为
`feat/scalable-3d-200v200`。工作树包含本轮尚未提交的 A2/A3 运行证据修复，因此不能
作为 clean formal 来源。规模化模块全量测试为 `338 passed, 1 warning`；D3、D4、D5、
D6 和跨模块合同分别为 `546 passed, 1 skipped`、`637 passed`、`682 passed`、
`1071 passed` 和 `8 passed`。警告来自本机 Matplotlib `Axes3D` 和显卡管理接口，不影响
本轮非图形结论。当前工作树还有三份受保护的未跟踪资料，正式实验必须使用绑定 clean
commit 的独立 worktree。

2026-07-27 增补：D5 当前运行时已在 clean commit `8d5e02e...b54` 上完成
external audit v2、bundle v5 和 post-assembly v2 证据闭环。该增补只更新 D5 合成预准入
状态；本报告其余 2026-07-26 实验进度和测试计数仍保留原审计时点口径。

2026-07-26 运行证据增补：D3 已传播统一 owner/epoch/lease；D4 owner ACK 可在同一
不可变绑定下供后续物理窗口复用；main 的 A2 正向用例形成 1 次 owner ACK、3 个非 hold
绑定、1 个物理窗口和 1 条安全采用证据。A3 正向探针形成 40 条运行确认、21 帧匿名观测
和 21 个物理观测窗口。两项都缺唯一同键 R0，因此只证明软件链可达，不证明学习收益或
模型授权。

2026-07-27 独立配对增补：main 已为 A2/A3 分别运行独立候选和规则基线 episode。
A2 的 20 个开发 seed 均有候选评估记录，但受控策略没有产生资源变化、保持状态或跨区
转移，可识别干预和实际采纳均为 0。原 18 次结论来自并行 D3 常规重规划，现已撤销。
随后修复了 D4 受约束开发适配器的正式裁决上下文传递。main 的 5 对 5、seed 1 探针
形成单个 `request_replan`、D3 严格后继计划、owner ACK、安全采用和物理窗口，在线
真值使用为 0。该适配器仍是 development-only，正式收益装配器拒绝其进入 A2 收益
审计，因此不能替换 20-seed 批次或形成模型收益结论。
A3 的 536 条候选动作均有配对处置，152 条可配对，384 条为候选物理窗口缺失，记录级
覆盖率为 28.36%。D6 不把 152 条子集表述为完整 A3 采用证据。该批 seed 未被核验为
未见集合，策略为受控测试夹具，D6 非退化、模型授权和运行权限仍为 false。
当前 main 已在未提交工作树上为 536/536 条候选补齐阶段证据。不落盘复跑把 384 条缺窗
拆为 344 条匿名观测/确认缺窗和 40 条细因未解析。后 40 条的观测清单在 episode 结束时
尚未闭合，不能标记为物理窗口不完整；没有发现 ACK、确认、命令过期、时序错配或反馈
缺失。该开发探针没有替换冻结批次，也没有改变完整 A3 指标不可用的结论。
本次修正后 D6 严格采用审计为 51 项通过，配对编排为 6 项通过，D4 全量为
`674 passed, 1 warning`，可扩展三维全量为 `346 passed, 1 warning`，跨模块合同为
`8 passed, 1 warning`。2026-07-27 收尾回归进一步确认：D3 全量
`551 passed, 1 skipped`，D5 全量 `726 passed, 2 warnings`，D6 全量
`1101 passed, 1 warning`，main 模块栈专项为 `76 passed`。警告来自既有 Matplotlib
三维投影和显卡管理接口环境，不改变本次非图形证据结论。

## 版本管理

当前长期分支结构符合既定方案：

```text
main
└── feat/scalable-3d-200v200
```

`main` 保留 2v2、5v5、M5N2 和 AirSim 基线，专项分支承载三维世界、D1-D7 扩展、图神经
网络和强化学习。D1-D7 没有拆成长驻模块分支，子智能体不操作 Git 索引，由 main 分批提交。
当前没有 `scalable3d-*` 阶段标签。完整 R0、学习模型准入和最终多随机种子验收尚未完成，
因此现在不满足创建 `scalable3d-v0.2.0` 及后续标签的条件。

episode manifest 已记录 Git 提交、配置摘要、场景版本、世界合同、总线合同、D3 策略、
D4 策略、D5 模型和阈值版本。实验矩阵另行绑定完整父清单、执行范围、分片、设备和 bundle
文件树。模型权重使用摘要和模型说明管理，训练中间检查点及大规模输出不作为普通源码提交。

## 目标逐项审计

| 目标 | 当前状态 | 现有证据 | 未完成工作 |
| --- | --- | --- | --- |
| 三维质点环境 | 已实现 | 北东地六维状态、速度/加速度/转弯/爬升约束、独立相机姿态、统一仿真时钟及单元测试已存在 | 200 对 200 当前实时倍率均值约 `0.1163`，距离实时仍约 8.6 倍 |
| 三维场景 | 已实现 | 匀速、转弯、交叉、编队、分裂、规避、高度变化、故障和通信场景已进入配置与实验矩阵 | 代表性 AirSim 子场景与三维正式矩阵仍需统一验收 |
| 视觉投影 | 已实现规则路径 | 针孔投影、边界框代理、视场/遮挡/噪声/漏检/虚警、双时间戳、像素协方差和匿名局部航迹已接线 | 合成固定候选图门限已通过；真实相机和重新构图条件下的跨视角泛化仍未验证 |
| D1-D2 密集三维跟踪 | 已实现规则路径 | 六维融合、异步量测、稀疏关联、身份承诺、真值隔离和 `id_switch_count` 合同已接入统一总线 | 完整 900 单元正式 R0、严格身份长期证据和系统实时仍未闭合 |
| D3 三维分配 | 已实现规则及影子学习路径 | 三维可达性、需求槽、M 对 N、版本化计划、迟滞、规则代价与有界学习修正均存在 | A1 实际采用、物理结果、同键 R0 非退化和 evidence assembler 尚未完成 |
| D4 区域降级 | 规则路径和 A2 独立配对软件链已实现 | 中心、二级、完全分布式、epoch/lease、主动降级、联盟确认、因果回执、A2 assembler、owner ACK 和物理窗口已接线；20 个开发 seed 保留无操作拒绝记录，真实受约束开发适配器的单 seed 正向运行桥已接通 | 实际可准入的非零学习策略、严格未见 seed、成对非退化和正式 A2 scope 尚未完成 |
| D5 图网络 | 当前运行时证据资格通过，运行未授权 | clean commit `8d5e02e...b54` 已形成 20-seed/900-episode/45-cell held-out、paired-shadow、900 条唯一 lineage、external audit v2、bundle v5 和 post-assembly v2；两级 D6 审计均为 `pass` | 六项运行权限仍为 false；真实相机泛化、中心身份绑定正确率、物理闭环和正式 G1 作用域尚未验证 |
| D5 主动视觉 | 规则路径、A3 assembler 和独立配对软件链已实现 | 观察目标、搜索/云台动作、数据集、行为克隆、安全回退、命令确认和匿名物理观测窗口合同存在；536 条候选均有处置和开发阶段证据，152 条可配对，344 条明确缺少匿名观测，40 条因清单未闭合保持细因未解析 | 实际获准模型、动作/观测节拍校准、严格未见 seed、物理非退化、近端策略优化准入和正式证据未完成 |
| D7 三维闭环 | 已实现规则路径 | 三维位置比例导引、视觉视线比例导引、视线/预计碰撞时间滤波、机动约束和资源状态回写已接线 | 正式完整矩阵的五米物理结果、实时性和代表性 AirSim 对照仍需补齐 |
| D6 规模化评估 | 软件能力已实现 | 逐 seed 指标、置信区间、三维图表、正式 scope 审计、D5 G1 外部审计和 A1/A2/A3 严格采用审计已存在；A2 无操作归因和 A3 完整候选分母均按失败关闭口径审计 | A1 同键 R0、A2/A3 正式未见模型对照、完整正式矩阵、最终 Word 报告和非退化结论未闭合 |
| 真值隔离和安全外壳 | 已实现并持续回归 | 在线真值使用、`global_track_id` 改写、过时计划、重复资源、联盟确认和控制许可均有失败关闭合同 | 仍需在全部正式学习 scope 中逐 cell 证明，而非只依赖单元测试 |
| AirSim 边界 | 符合计划 | 2v2、5v5、M5N2 和代表性子场景保留在既有 runtime；未尝试 200 对 200 AirSim 物理运行 | 最终只选择 5 至 20 架代表性场景做外部验证 |

## 实施阶段

| 阶段 | 状态 | 判断 |
| --- | --- | --- |
| 1. 分支、计划和合同 | 完成 | 分支、PLAN、版本合同和真值边界已冻结并持续升级 |
| 2. 三维世界、相机和通信 | 完成 | 代码、测试和规则运行均存在 |
| 3. 200 对 200 真值传播 | 完成开发验证 | 无硬编码和有限状态已验证，实时目标未达到 |
| 4. 接入规则 D1-D3 | 完成开发验证 | 已不再停留在早期约 34 条航迹状态；正式 R0 尚未完整重跑 |
| 5. 接入 D7 三维闭环 | 完成开发验证 | 控制状态已回写世界；正式物理闭环证据仍部分完成 |
| 6. 建设 D5 匿名视觉数据 | 完成 | 数据集、规范切分、困难样本和审计链存在 |
| 7. 训练并验证 D5 图网络 | 合成预准入完成 | 当前运行时模型、20-seed 证据、v5 和两级 D6 审计均已闭合；在线 G1 授权和现实证据未完成 |
| 8. D3 学习代价修正 | 部分完成 | 行为克隆、影子运行和安全外壳存在，强化学习正式准入未完成 |
| 9. 区域调度和主动视觉学习 | 部分完成 | A2/A3 assembler、运行确认、物理窗口和独立 R0 开发配对已接通；实际模型授权、严格未见 seed、非退化、正式准入与消融未完成 |
| 10. D4 故障场景 | 完成规则验证 | 中心、二级和分布式规则闭环已接入；学习区域策略未准入 |
| 11. D6 多 seed 验收 | 部分完成 | 描述性 20-seed 和多项专项正式评审存在，完整七变体矩阵未完成 |
| 12. 文档、报告和合并 | 进行中 | README、PLAN、GAP 和系统总纲持续同步；最终报告、标签和合并 main 尚未执行 |

## 验收目标

| 验收项 | 当前结论 |
| --- | --- |
| 200 目标与 200 资源无硬编码、数组越界和非有限状态 | 开发及部分正式单元通过；完整 900 单元 R0 尚未闭合 |
| 在线真值使用为 0 | 当前规则主线和已完成正式单元通过；学习正式 scope 尚无 episode |
| `global_track_id` 非法改写为 0 | 当前回归通过；学习正式 scope 尚待证明 |
| 过时计划、重复授权和硬约束违规为 0 | 当前回归通过；学习正式 scope 尚待证明 |
| 名义目标航迹召回率不低于 95% | 现有局部证据不能代替完整正式矩阵，保持待验收 |
| D5 edge F1 不低于 90%，错误合并率不高于 1% | 当前合成 held-out 和五类固定候选图扰动通过；真实相机及重新构图压力条件仍待验收 |
| 高威胁需求满足率不低于 95% | 规则质点专项已有通过结果，完整正式矩阵待验收 |
| 学习方案不增加身份交换、重复分配和安全违规 | 尚无获准学习模型和正式学习 episode，未验证 |
| 三维距离不超过 5 米计为物理成功 | 判据已实现；完整正式矩阵待验收 |
| 至少 20 个未见 seed 与置信区间 | 数据切分和部分评估已实现；七变体正式比较未完成 |
| 学习显存不超过 5 GiB | 尚缺冻结 RTX 4050 环境的正式 G1/A1/A2/A3 画像 |
| 200 对 200 不低于实时 | 未达到；现有均值约为实时的 `0.1163` |

## 正式实验状态

正式父清单包含 R0、G1、A1、A2、A3、C1、F1 共 5700 个单元。R0 scope 为 900 个单元。
修复后的 clean source 已完成 shards 0、5、9，共 `135/900`。学习模型均无 assist 权限，
因此 G1、A1、A2、A3、C1、F1 正式 episode 均为 0。

当前规模化输出约 18 GiB。2026-07-27 实测文件系统可用空间约 15 GiB，已低于正式
运行器的 20 GiB 保护下限。下一轮正式分片不能直接启动。现有正式证据不得删除、覆盖
或跨提交拼接。
`artifact_archive.py` 已提供逐文件清单、原子复制和双端复核，且不包含删除入口。当前
仍缺外部大容量目标目录。继续运行前需要完成以下任一项：

1. 将既有正式输出连同 SHA-256 清单迁移到可验证的外部存储；
2. 扩容当前文件系统；
3. 经用户明确授权，按证据保留清单压缩或清理可重建的开发输出。

## 当前关键路径

1. main 定义独立于 D5 v5 和 D6 审计的人工批准实验授权包。授权包只允许 G1 在既有
   候选边上受控评分，带作用域、有效期和撤销条件，不授予身份所有权、分配、降级或
   控制权限。随后运行同键 R0/G1 配对作用域。
2. D3/A1 继续形成可辨识离散绑定变化、物理结果和同键 R0。D4/A2 独立开发配对已完成，
   真实受约束开发适配器已形成严格后继计划、运行确认和物理窗口。下一步使用实际获准
   模型和严格未见 seed 完成 20-seed 成对非退化；development adapter 和现有无操作
   测试策略夹具均不就地晋级。
3. D5/A3 独立开发配对和阶段删失拆分已完成。下一步调整主动视觉动作采样与匿名观测
   节拍，提高命令窗口和图像帧的有效对应率，并从 clean commit 冻结新批次，再在实际
   获准主动视觉模型下完成未见 seed 非退化审计。
4. 解决正式输出存储后，从绑定当前 clean commit 的计划继续或重建完整 R0，并按准入顺序
   运行 G1、A1、A2、A3、C1、F1。
5. D6 生成逐 seed CSV、聚合 JSON、中文 Markdown、Word、三维曲线和动画，完成最终
   需求逐项验收。

当前阶段不应创建 `scalable3d-v1.0.0` 标签，也不应把 development/shadow 模型、正向
fixture 或规则回退结果记入学习组。
