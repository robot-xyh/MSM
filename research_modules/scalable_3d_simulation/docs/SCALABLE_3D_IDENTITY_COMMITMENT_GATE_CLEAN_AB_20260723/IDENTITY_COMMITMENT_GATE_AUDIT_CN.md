# 身份承诺下游准入复核

## 结论

clean seed 1100 同输入复跑通过下游安全合同。D2 撤回身份承诺后，D3 在同一规划周期强制升版并撤回相关分配。D5 主动视觉、D5 终端绑定和 D7 导引没有继续消费这些目标。

身份中性质心候选没有形成实际处理。46 个候选组件全部关闭，结果与控制臂相同。该候选保持默认关闭，不能进入多随机种子晋级。

## 条件

- 日期：2026-07-23
- Git 提交：`7e15dac9cdaf6743999dfe045a70676fd31a17d6`
- 工作树：detached clean，`repository_dirty=false`
- 场景：200 对 200
- 侦察节点：2
- 仿真时长：2.200000 秒
- seed：1100
- 配置 SHA-256：`20ef5248c8b45ff5aced9080c8d47e65a43aaba54f18ce824dc50fac7a52b840`

控制臂启用不透明来源键和结构歧义保活。候选臂只增加身份中性质心校正。两臂离线真值标签和状态哈希一致，在线真值使用均为 0。

## 撤回链路

t=1.000000 秒时，D3 检出 11 个原计划目标不再处于 `committed` 状态。计划从 v1 严格升为 v2，分配数由 193 降为 186。该次撤回绕过迟滞，原因记录为 `previous_target_identity_uncommitted`。

| 下游检查 | 违规目标数 |
| --- | ---: |
| D3 后续分配 | 0 |
| D5 主动视觉 | 0 |
| D5 终端绑定 | 0 |
| D7 导引 | 0 |

本 episode 没有注入过时计划。过时计划拒绝由运行时和模块回归测试覆盖，本报告不把未发生的输入写成 episode 实测结果。

## 指标

| 指标 | hold 控制臂 | hold + 质心 | 判断 |
| --- | ---: | ---: | --- |
| 严格身份交换 | 3 | 3 | 相同 |
| 航迹连续性 | 0.826667 | 0.826667 | 相同 |
| 覆盖连续性 | 0.828333 | 0.828333 | 相同 |
| 重复分配 | 0 | 0 | 相同 |
| 可用身份映射 | 1491 | 1491 | 相同 |
| 常规不可用映射 | 218 | 218 | 相同 |
| 未承诺映射 | 76 | 76 | 相同 |
| 身份承诺覆盖率 | 0.957471 | 0.957471 | 相同 |

两臂 D1/D2/D3 终态均为 `202/201/186`。未承诺来源绑定和候选绑定违规均为 0。

## 候选状态

- 候选组件：46
- 已施加组件：0
- 拒绝组件：46
- 拒绝原因：`{"oosm_scan": 30, "unbalanced_component": 16}`
- 最大平移：0.000000 米

本轮关闭的是 D2 身份承诺到 D3、D5、D7 的执行安全门。D1 候选仍为零 treatment，结构歧义场景下的连续性和可用映射没有恢复。后续先做冻结扫描重放，形成可复现的非零处理窗口，再决定是否重跑未见 seed。

## 审计

- 合同门：`通过`
- 算法晋级：`不允许`
- 原因：`zero_effective_treatment`
- 违规项：`[]`
