# 可扩展三维学习来源预生成检查

## 结论

2026-08-02 当前共享工作区的跨模块预检状态为
`blocked_by_dirty_generation_worktree`。D3、D4、D5 的冻结计划、生产器适配和来源生成
请求均为 `3/3` 就绪；唯一 blocker 是待提交变更。main 执行授权、正式来源生成、训练、
验证消费和运行控制仍为 false。前置专项为 `25 passed`。

本轮软件证据包括 D3 的 15/15 首配方实际 stage，D4 的 324/324 配方与 972 帧审计，
以及 D5 的 104/104 配方容量审计和五个探针 693 帧。D4 sequence 0/1 和 D5 临时完成数
`1 -> 2` 均验证了跨进程恢复。上述结果未形成正式来源数据集，也未读取 future-held-out
payload。

以下 2026-08-01 内容保留为上一阶段 clean preflight 历史记录。

2026-08-01 从干净 detached worktree `f0819e056094c5d8e090a653678dae3899037481`
重跑跨模块预检，状态为 `blocked_by_source_generation_request`。D3、D4、D5 的冻结计划、
生产器适配和干净工作树门均已通过。三个来源生成请求仍为 `false`，因此执行计划、生成授权
和全部运行权限继续关闭。

本轮只执行受控内存 probe。没有生成 300/324/104 episode 来源清单，没有写入样本或模型，
没有读取正式 seed `1000-1019`，也没有运行 shard 10-19。既有正式 R0 `450/900` 结论未
修改。干净工作树中的适配器与预检专项为 `9 passed`。

## 登记状态

全局登记表保护既有 658 个 seed，并保留 728 个新 seed。D3 使用 300 个，D4 使用 324 个，
D5 使用 104 个。来源合同路径、SHA-256、split 和 seed 互斥关系均通过只读校验。新分配与
正式 seed 的交集为 0。

| 模块 | 冻结计划 | 生产器适配 | 内存 probe | 生成请求 |
| --- | :---: | :---: | ---: | :---: |
| D3 A1 v3 | 通过 | 通过 | 1 episode / 1 帧 | 关闭 |
| D4 A2 v8 | 通过 | 通过 | 3 episode / 9 帧 | 关闭 |
| D5 A3 v3 | 通过 | 通过 | 5 episode / 493 帧 | 关闭 |

2026-08-02 的当前请求状态为：

| 模块 | 计划数量 | 当前来源可行性证据 | 请求状态 | 正式生成 |
| --- | ---: | --- | :---: | ---: |
| D3 A1 v3 | 300 | 15/15 首配方满足 `3/3/2` 并 stage | 就绪 | 0 |
| D4 A2 v8 | 324 | 324/324、972 帧、跨进程恢复通过 | 就绪 | 0 |
| D5 A3 v3 | 104 | 104/104、五探针 693 帧、前缀恢复通过 | 就绪 | 0 |

## 适配证据

### D3

生产器可逐条读取 300 条日程，映射 13 个场景族和非等量目标/资源数量。实际主状态机帧已
转换为 A1 v3 适配对象，量测时间早于到达时间，需求槽、候选边和最终计划边保持一致。严格
writer 合同可加载，但本轮没有创建正式数据集目录或 manifest。

### D4

生产器可读取 324 条 TRAIN 配方，映射 8、9、12、16 区域拓扑、三类通信条件和三类供需
边界。内存 probe 分别覆盖安全正向转移、安全反向转移和困难无转移负样本。9 个实际运行帧
均通过 v8 在线/离线对象校验，在线真值使用为 0，writer 未完成正式清单。

### D5

生产器可读取 104 条三 split 配方，执行四段意图窗口、拦截机/侦察机角色和五类困难混淆。
5 个非正式 probe 共检查 493 个主动视觉运行帧，各窗口达到 24 个唯一样本下限。近似并列
目标窗口具有至少两个合法中心航迹投影，在线对象未读取真值身份，也未创建或改写
`global_track_id`。

## 阻断项

- 当前工作区尚未分批提交，clean-source 门未通过。
- D3、D4、D5 尚未获得 main generation-only 执行授权。
- 正式分区目录、全量 manifest 和 D6 来源审计尚不存在。

## 后续门控

1. 分批提交当前 main、D3、D4、D5 和文档变更，从新的干净提交再次预检。
2. clean preflight 通过后，单独签发只允许数据集生成的 main 授权。
3. 依次、限量、可恢复地生成 D3 300、D4 324 和 D5 104 条来源。
4. 生成完成后由 D6 只读检查 seed、split、样本唯一性、双时间戳、在线真值和来源哈希。

当前所有生成、训练、验证、测试、影子、辅助、运行、分配、降级、联盟、控制、物理执行及
全局航迹编号写权限均为 `false`。
